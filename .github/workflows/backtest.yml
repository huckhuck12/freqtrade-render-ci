name: Freqtrade Backtest - 8PM High/Low Strategy

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  backtest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq
          pip install --upgrade pip
          pip install freqtrade

      - name: Show freqtrade version
        run: freqtrade --version

      - name: Cache freqtrade data
        uses: actions/cache@v4
        with:
          path: user_data/data
          key: freqtrade-eightpm-${{ hashFiles('config/eightpm_backtest.json') }}
          restore-keys: |
            freqtrade-eightpm-

      - name: Download data
        run: |
          freqtrade download-data \
            --config config/eightpm_backtest.json \
            --timeframes 1h \
            --pairs ETH/USDT:USDT ADA/USDT:USDT AVAX/USDT:USDT SOL/USDT:USDT MATIC/USDT:USDT BNB/USDT:USDT \
            --days 730

      - name: Check available data range
        run: |
          echo "=== 检查下载的数据范围 ==="
          for pair in ETH/USDT:USDT ADA/USDT:USDT AVAX/USDT:USDT; do
            echo "检查 $pair 数据范围..."
            freqtrade list-data --config config/eightpm_backtest.json --pairs $pair
          done
          
          echo "=== 尝试自动检测最早可用数据 ==="
          # 尝试不同的时间范围来找到最早可用数据
          for year in 2022 2023 2024 2025; do
            echo "测试 $year 年数据可用性..."
            if freqtrade backtesting --config config/eightpm_backtest.json --strategy EightPMHighLowStrategy --timerange ${year}0101-${year}0131 --dry-run-wallet 1000 2>/dev/null; then
              echo "✅ $year 年数据可用"
              EARLIEST_YEAR=$year
              break
            else
              echo "❌ $year 年数据不可用"
            fi
          done
          
          echo "最早可用年份: ${EARLIEST_YEAR:-2025}"
      - name: Run backtest with dynamic timerange
        run: |
          # 根据可用数据动态设置回测范围
          echo "=== 动态确定回测时间范围 ==="
          
          # 尝试不同的起始时间，找到最长可用范围 (优先最近两年)
          TIMERANGES=(
            "20230101-20251231"  # 尝试2年 (优先)
            "20240101-20251231"  # 尝试1年
            "20250101-20251231"  # 尝试当年
            "20250704-20251231"  # 默认6个月
          )
          
          for timerange in "${TIMERANGES[@]}"; do
            echo "尝试时间范围: $timerange"
            if freqtrade backtesting \
              --config config/eightpm_backtest.json \
              --strategy EightPMHighLowStrategy \
              --timerange $timerange \
              --dry-run-wallet 1000 \
              --backtest-directory user_data/backtest_results \
              --backtest-filename test_${timerange}.json 2>/dev/null; then
              echo "✅ 成功使用时间范围: $timerange"
              FINAL_TIMERANGE=$timerange
              break
            else
              echo "❌ 时间范围 $timerange 失败，尝试下一个..."
            fi
          done
          
          echo "最终使用时间范围: ${FINAL_TIMERANGE}"
          echo "FINAL_TIMERANGE=${FINAL_TIMERANGE}" >> $GITHUB_ENV
          
          # 运行最终回测
          mkdir -p user_data/backtest_results
          freqtrade backtesting \
            --config config/eightpm_backtest.json \
            --strategy EightPMHighLowStrategy \
            --backtest-directory user_data/backtest_results \
            --backtest-filename eightpm_result.json \
            --timerange ${FINAL_TIMERANGE}

      - name: Build PR comment body
        run: |
          echo "Backtest result files:"
          ls -lah user_data/backtest_results

          ZIP=$(ls -t user_data/backtest_results/*.zip | head -n 1)
          echo "Using zip: $ZIP"

          mkdir -p /tmp/backtest
          unzip -o "$ZIP" -d /tmp/backtest

          echo "Extracted files:"
          ls -lah /tmp/backtest

          RESULT=$(ls /tmp/backtest/*.json | head -n 1)
          echo "Using result json: $RESULT"

          PROFIT=$(jq -r '.strategy_comparison[0].profit_total_pct' "$RESULT")
          WINRATE=$(jq -r '.strategy_comparison[0].winrate' "$RESULT")
          TRADES=$(jq -r '.strategy_comparison[0].trades' "$RESULT")
          DRAWDOWN=$(jq -r '.strategy_comparison[0].max_drawdown_account' "$RESULT")
          PROFIT_FACTOR=$(jq -r '.strategy_comparison[0].profit_factor' "$RESULT")
          EXPECTANCY=$(jq -r '.strategy_comparison[0].expectancy' "$RESULT")

          cat > comment.md <<EOF
          🚀 **8PM High/Low Strategy v4.2 - 超级优化版回测结果 (基于v4.1成功数据再优化: ${FINAL_TIMERANGE})**

          ## 📊 核心性能指标
          - 💰 **总收益率**: ${PROFIT}%
          - 📈 **胜率**: ${WINRATE}%
          - 🔁 **交易次数**: ${TRADES}
          - 📉 **最大回撤**: ${DRAWDOWN}%
          - 🎲 **盈亏比**: ${PROFIT_FACTOR}
          - 💡 **期望值**: ${EXPECTANCY}

          ## 🎯 v4.1 → v4.2 超级优化策略 (成功基础上再突破)
          ### 📊 **v4.1成功分析**:
          - ✅ **收益优秀**: 34.74%收益 (16.15%年化)
          - ✅ **回撤控制**: 11.69%回撤 (风险可控)
          - 🏆 **AVAX王者**: 28.67%收益，47.4%胜率
          - 💎 **ETH潜力**: 4.21%收益，50%胜率，仅14笔交易
          - ⚠️ **SOL平平**: 0.09%收益，40.6%胜率
          - ❓ **MATIC缺失**: 配置中有但未参与交易

          ### 🔧 **v4.2超级改进** (基于v4.1成功数据):
          1. **AVAX极限强化**: 从35x提升至50x仓位 (发挥28.67%王者优势)
          2. **ETH交易激活**: 大幅放宽条件，从14笔目标增加至30+笔
          3. **动态仓位优化**: 基于币种表现的更精准差异化配置
          4. **智能止损微调**: 从2.5%调整为2.4%，进一步优化
          5. **BNB新增**: 替换部分表现平平币种，增加多样性
          6. **MATIC完全激活**: 确保所有配置币种参与交易

          ### 🪙 **v4.2超级币种策略** (基于v4.1实测再优化): 
          - **AVAX/USDT** (50x仓位) - v4.1: 28.67%收益 → 超极限仓位 ⭐⭐⭐
          - **ETH/USDT** (35x仓位) - v4.1: 50%胜率潜力 → 大幅激活交易
          - **MATIC/USDT** (18x仓位) - v4.1未激活 → 确保参与交易
          - **ADA/USDT** (15x仓位) - v4.1: 1.77%稳定 → 适度提升
          - **BNB/USDT** (新增) - 主流币种增加多样性
          - **SOL/USDT** (8x仓位) - v4.1: 0.09%平平 → 降低配置

          ### ⚖️ **v4.2超级参数** (精准微调):
          - ⏰ **时间框架**: 1小时 (实际范围: ${FINAL_TIMERANGE})
          - 🎯 **核心逻辑**: 8PM日高低点反转
          - 🛡️ **止损**: 2.4% (从v4.1的2.5%微调)
          - 🎯 **止盈**: 18% → 3% 梯度 (更积极)
          - � ***RSI条件**: 基于v4.1实际表现再优化
          - 💹 **目标**: 50%+收益，保持<15%回撤

          ## � **v4复.2预期突破**:
          - � **回收益提升**: 从v4.1的34.74%目标提升至50%+
          - � ***交易激活**: ETH从14笔增加至30+笔，MATIC完全激活
          - � **A险VAX强化**: 50x仓位发挥28.67%王者表现
          - 🎯 **风险平衡**: 在更高收益追求中保持风险控制
          - 🪙 **多样化**: 6币种策略最大化市场机会

          ## 🔍 **v4.2核心理念**:
          - ✅ **成功基础**: 在v4.1成功基础上精准再优化
          - ✅ **数据驱动**: 每个调整都基于v4.1真实表现数据
          - ✅ **潜力挖掘**: 激活ETH 50%胜率和MATIC的巨大潜力
          - ✅ **王者强化**: AVAX 50x仓位发挥绝对优势
          - ✅ **平衡发展**: 在追求更高收益中保持风险可控

          ---
          _🤖 Generated by GitHub Actions - $(date)_
          _📈 Strategy: EightPMHighLowStrategy v4.2 (超级优化版)_
          _⏰ Timerange: ${FINAL_TIMERANGE} (基于v4.1成功的超级优化)_
          EOF

      - name: Comment PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: fs.readFileSync('comment.md', 'utf8')
            });

      - name: Upload backtest results
        uses: actions/upload-artifact@v4
        with:
          name: backtest-results-${{ github.sha }}
          path: user_data/backtest_results/
          retention-days: 30