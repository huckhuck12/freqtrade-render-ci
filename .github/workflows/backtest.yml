name: Freqtrade Backtest - 8PM High/Low Strategy

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  backtest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq
          pip install --upgrade pip
          pip install freqtrade

      - name: Show freqtrade version
        run: freqtrade --version

      - name: Cache freqtrade data
        uses: actions/cache@v4
        with:
          path: user_data/data
          key: freqtrade-eightpm-${{ hashFiles('config/eightpm_backtest.json') }}
          restore-keys: |
            freqtrade-eightpm-

      - name: Download data
        run: |
          freqtrade download-data \
            --config config/eightpm_backtest.json \
            --timeframes 1h \
            --pairs ETH/USDT:USDT ADA/USDT:USDT AVAX/USDT:USDT \
            --days 180

      - name: Run backtest
        run: |
          mkdir -p user_data/backtest_results
          freqtrade backtesting \
            --config config/eightpm_backtest.json \
            --strategy EightPMHighLowStrategy \
            --backtest-directory user_data/backtest_results \
            --backtest-filename eightpm_result.json \
            --timerange 20250704-20251231

      - name: Build PR comment body
        run: |
          echo "Backtest result files:"
          ls -lah user_data/backtest_results

          ZIP=$(ls -t user_data/backtest_results/*.zip | head -n 1)
          echo "Using zip: $ZIP"

          mkdir -p /tmp/backtest
          unzip -o "$ZIP" -d /tmp/backtest

          echo "Extracted files:"
          ls -lah /tmp/backtest

          RESULT=$(ls /tmp/backtest/*.json | head -n 1)
          echo "Using result json: $RESULT"

          PROFIT=$(jq -r '.strategy_comparison[0].profit_total_pct' "$RESULT")
          WINRATE=$(jq -r '.strategy_comparison[0].winrate' "$RESULT")
          TRADES=$(jq -r '.strategy_comparison[0].trades' "$RESULT")
          DRAWDOWN=$(jq -r '.strategy_comparison[0].max_drawdown_account' "$RESULT")
          PROFIT_FACTOR=$(jq -r '.strategy_comparison[0].profit_factor' "$RESULT")
          EXPECTANCY=$(jq -r '.strategy_comparison[0].expectancy' "$RESULT")

          cat > comment.md <<EOF
          🚀 **8PM High/Low Strategy v2.5 - 混合平衡优化版回测结果**

          ## 📊 核心性能指标
          - 💰 **总收益率**: ${PROFIT}%
          - 📈 **胜率**: ${WINRATE}%
          - 🔁 **交易次数**: ${TRADES}
          - 📉 **最大回撤**: ${DRAWDOWN}%
          - 🎲 **盈亏比**: ${PROFIT_FACTOR}
          - 💡 **期望值**: ${EXPECTANCY}

          ## 🎯 v2.5 混合平衡优化策略
          ### 📊 **基于v2.4数据的精准调整**:
          1. **币种精选**: 质量胜过数量，专注3个高质量币种
          2. **参数平衡**: v2.3和v2.4之间的最优平衡点
          3. **止损优化**: 1.8%平衡止损，减少不必要止损
          4. **仓位智能**: 基于实际表现的差异化配置

          ### 🪙 **v2.5精选币种** (基于v2.4表现): 
          - **ADA/USDT** (2.5x仓位) - v2.4最佳: 66.7%胜率, 0.13%收益
          - **ETH/USDT** (1.5x仓位) - 需要恢复: 从70%降至57.1%胜率
          - **AVAX/USDT** (1.2x仓位) - 新币种最佳: 55%胜率, 0.04%收益

          ### ❌ **v2.5移除币种** (基于v2.4表现):
          - **DOT**: 50%胜率, -0.06%收益 → 表现差
          - **LINK**: 58.8%胜率, 0.03%收益 → 收益低

          ### ⚖️ **v2.5平衡参数**:
          - ⏰ **时间框架**: 1小时
          - 🎯 **核心逻辑**: 8PM日高低点反转
          - 🛡️ **止损**: 1.8% (v2.3和v2.4的平衡点)
          - 🎯 **止盈**: 5.5% (平衡收益和胜率)
          - 📊 **目标胜率**: 65%+
          - 💹 **目标收益率**: 0.25%+

          ## 📊 **版本演进对比**:
          | 指标 | v2.3 | v2.4 | v2.5目标 | 优化策略 |
          |------|------|------|----------|----------|
          | 收益率 | 0.20% | 0.15% | **0.25%+** | 精选币种+平衡参数 |
          | 胜率 | 67.6% | 57.0% | **65%+** | 提高信号质量 |
          | 交易数 | 37笔 | 86笔 | **50-60笔** | 平衡频率和质量 |
          | 币种数 | 5个 | 5个 | **3个** | 质量胜过数量 |

          ## 🔍 **v2.5核心优势**:
          - ✅ **数据驱动**: 基于v2.4真实回测数据精准调整
          - ✅ **质量优先**: 3个精选币种胜过5个平庸币种
          - ✅ **参数平衡**: v2.3稳定性 + v2.4活跃度
          - ✅ **智能仓位**: ADA最大配置，ETH标准配置
          - ✅ **风险控制**: 减少止损，提升胜率

          ---
          _🤖 Generated by GitHub Actions - $(date)_
          _📈 Strategy: EightPMHighLowStrategy v2.5_
          EOF

      - name: Comment PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: fs.readFileSync('comment.md', 'utf8')
            });

      - name: Upload backtest results
        uses: actions/upload-artifact@v4
        with:
          name: backtest-results-${{ github.sha }}
          path: user_data/backtest_results/
          retention-days: 30